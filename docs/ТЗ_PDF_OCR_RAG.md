# Техническое задание (ТЗ)
# Проект: Pdf_scan_and_ocr — конвертация сканированных PDF в PDF/A с RAG-поиском

| Поле | Значение |
|------|----------|
| **Версия документа** | 1.0 |
| **Дата** | 16.06.2026 |
| **Платформа** | Windows 10 / Windows 11 |
| **Язык разработки** | Python 3.11+ |
| **Тип приложения** | Desktop GUI, модульная архитектура |
| **Репозиторий** | Pdf_scan_and_ocr |

---

## 1. Назначение и цели проекта

### 1.1. Назначение

Разработать настольное GUI-приложение для автоматизированной обработки отсканированных PDF-документов: постраничное распознавание текста с помощью LLM OCR, формирование поисковых PDF/A, векторизация содержимого для RAG-базы и интеллектуальный поиск по документам через LLM-агента.

### 1.2. Цели

1. Преобразование сканированных PDF в **PDF/A** с **встроенным текстовым слоем** (полнотекстовый поиск в PDF-ридере).
2. Сохранение полного распознанного текста в **Markdown** для последующей обработки и RAG.
3. Построение **векторной базы знаний** (RAG) на основе содержимого документов.
4. Предоставление **чат-интерфейса** для семантического поиска с цитированием источников и ссылками на файлы.
5. Модульное разделение функций: обработка, управление RAG, поисковый агент.
6. Работа с LLM-моделями, развёрнутыми **на отдельных ПК в локальной сети** (доступ по IP и порту).

### 1.3. Целевая аудитория

Пользователи, работающие с архивами отсканированных документов (делопроизводство, юридические отделы, библиотеки, личные архивы), которым необходимы поиск по содержимому и интеллектуальные ответы на вопросы по корпусу документов.

---

## 2. Общая архитектура

### 2.1. Принципы архитектуры

| Принцип | Описание |
|---------|----------|
| **Модульность** | Три независимых функциональных модуля с общим ядром (core) |
| **Слабая связанность** | Модули взаимодействуют через общие сервисы, конфигурацию и файловую структуру |
| **Расширяемость** | Возможность добавления новых моделей, провайдеров API, форматов экспорта |
| **Локальность данных** | Все документы и векторная БД хранятся локально на ПК пользователя |
| **Сетевые LLM** | OCR, embedding и поисковый LLM — внешние сервисы в LAN по HTTP API |

### 2.2. Высокоуровневая схема

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        GUI Application (Windows)                       │
├──────────────┬──────────────────────┬───────────────────────────────────┤
│  Модуль 1    │      Модуль 2        │         Модуль 3                  │
│  Обработка   │  Управление RAG      │    Поисковый агент                │
│  PDF         │                      │                                   │
└──────┬───────┴──────────┬───────────┴──────────────┬──────────────────┘
       │                  │                            │
       ▼                  ▼                            ▼
┌──────────────────────────────────────────────────────────────────────────┐
│                         Core Services Layer                               │
│  ConfigManager │ FileWatcher │ JobQueue │ VectorStore │ DocumentRegistry │
└──────┬─────────────────┬──────────────────────┬──────────────────────────┘
       │                 │                      │
       ▼                 ▼                      ▼
┌─────────────┐  ┌──────────────┐      ┌─────────────────┐
│  LAN API    │  │  LAN API     │      │  LAN API        │
│  Qwen3-VL   │  │  Qwen3-      │      │  LLM (поисковый │
│  30B OCR    │  │  Embedding   │      │  агент)         │
│  IP:Port    │  │  8B          │      │  IP:Port        │
└─────────────┘  └──────────────┘      └─────────────────┘
```

### 2.3. Рекомендуемый технологический стек

| Компонент | Технология | Обоснование |
|-----------|------------|-------------|
| GUI-фреймворк | **PySide6** (Qt6) или **CustomTkinter** | Нативный вид на Windows, богатые виджеты, поддержка tooltips |
| Асинхронность | **asyncio** + **QThread** / **threading** | Неблокирующий UI при длительной OCR и embedding |
| Мониторинг каталогов | **watchdog** | Отслеживание появления PDF в рабочем каталоге |
| PDF-обработка | **PyMuPDF (fitz)**, **pikepdf**, **ocrmypdf** (опционально) | Рендер страниц, встраивание текста, PDF/A |
| PDF/A конвертация | **pikepdf** + **Ghostscript** (внешняя зависимость) | Соответствие стандарту PDF/A |
| Векторная БД | **ChromaDB** или **Qdrant** (локальный режим) | Локальное хранение, метаданные, фильтрация |
| HTTP-клиент | **httpx** / **aiohttp** | Асинхронные запросы к LAN API |
| Конфигурация | **pydantic-settings** + **YAML/JSON** | Валидация настроек, профили подключения |
| Логирование | **loguru** | Ротация логов, уровни детализации |
| Упаковка | **PyInstaller** / **cx_Freeze** | Дистрибутив для Windows 10/11 |

### 2.4. Структура репозитория (целевая)

```
Pdf_scan_and_ocr/
├── docs/
│   └── ТЗ_PDF_OCR_RAG.md          # Настоящий документ
├── src/
│   ├── main.py                     # Точка входа
│   ├── app/
│   │   ├── application.py          # QApplication, главное окно
│   │   └── styles/                 # Темы, QSS
│   ├── core/
│   │   ├── config/                 # Менеджер настроек
│   │   ├── api/                    # Клиенты LAN API (OCR, embed, LLM)
│   │   ├── pdf/                    # Рендер, PDF/A, TOC
│   │   ├── rag/                    # Chunking, vectorstore, metadata
│   │   └── jobs/                   # Очередь задач, воркеры
│   ├── modules/
│   │   ├── pdf_processor/          # Модуль 1
│   │   ├── rag_manager/            # Модуль 2
│   │   └── search_agent/           # Модуль 3
│   └── shared/
│       ├── widgets/                # Общие виджеты (индикатор API, tooltips)
│       └── utils/
├── data/                           # Рабочие каталоги (настраиваемые)
│   ├── inbox/                      # Входящие PDF
│   ├── processing/                 # В обработке
│   └── documents/                  # Готовые документы
├── config/
│   └── default_settings.yaml
├── tests/
├── requirements.txt
└── README.md
```

---

## 3. Внешние LLM-сервисы (LAN API)

### 3.1. OCR — Qwen3-VL-30B-A3B-Instruct

- **Модель:** [Qwen/Qwen3-VL-30B-A3B-Instruct](https://huggingface.co/Qwen/Qwen3-VL-30B-A3B-Instruct)
- **Назначение:** Постраничное распознавание текста, определение структуры (заголовки, содержание, таблицы).
- **Развёртывание:** Отдельный ПК в LAN; приложение подключается по `http://<IP>:<PORT>`.
- **Ожидаемый интерфейс API** (адаптер в приложении должен поддерживать OpenAI-compatible и кастомный эндпоинт):

| Параметр | Описание |
|----------|----------|
| `base_url` | Базовый URL сервиса |
| `endpoint` | `/v1/chat/completions` или кастомный `/ocr/recognize` |
| `timeout_sec` | Таймаут запроса (по умолчанию 120 с на страницу) |
| `max_retries` | Число повторов при сетевой ошибке |
| `image_format` | PNG/JPEG, DPI рендера страницы |
| `prompt_template` | Настраиваемый промпт для OCR |

**Входные данные на страницу:** изображение страницы (base64 или multipart), номер страницы, язык (опционально).

**Выходные данные:** распознанный текст (plain/markdown), метаданные структуры (заголовки H1–H3, блок «Содержание»), уровень уверенности (если API отдаёт).

### 3.2. Embedding — Qwen3-Embedding-8B

- **Модель:** [Qwen/Qwen3-Embedding-8B](https://huggingface.co/Qwen/Qwen3-Embedding-8B)
- **Назначение:** Векторизация чанков текста для RAG.
- **Развёртывание:** Отдельный ПК в LAN, `http://<IP>:<PORT>`.

| Параметр | Описание |
|----------|----------|
| `base_url` | Базовый URL |
| `endpoint` | `/v1/embeddings` или `/embed` |
| `batch_size` | Размер пакета чанков (1–64) |
| `dimensions` | Размерность вектора (если модель поддерживает truncate) |
| `normalize` | L2-нормализация векторов |

### 3.3. Поисковый LLM-агент

- **Назначение:** Генерация ответов на основе top-K результатов RAG.
- **Развёртывание:** LAN API (модель на выбор администратора; рекомендуется модель семейства Qwen3).
- **Режим ответа:** **streaming** (потоковая передача токенов) для постепенного отображения текста в чате.

| Параметр | Описание |
|----------|----------|
| `base_url`, `endpoint` | Подключение к chat/completions |
| `stream` | `true` — обязательно для UI |
| `temperature`, `top_p`, `max_tokens` | Настраиваемые параметры генерации |
| `system_prompt` | Роль агента, правила цитирования источников |

### 3.4. Индикатор подключения к API

Для каждого внешнего сервиса (OCR, Embedding, Search LLM) в GUI отображается:

| Состояние | Визуализация | Поведение |
|-----------|--------------|-----------|
| **Подключено** | Зелёный индикатор | Успешный health-check / тестовый запрос < 5 с назад |
| **Проверка** | Жёлтый, анимация | Идёт ping или запрос |
| **Отключено** | Красный | Ошибка сети, таймаут, HTTP 4xx/5xx |
| **Не настроено** | Серый | IP/порт не заданы |

Периодическая проверка: каждые **30 с** (настраиваемо). Ручная кнопка «Проверить подключение» на панели каждого модуля.

---

## 4. Файловая структура данных

### 4.1. Каталоги модуля обработки PDF (настраиваемые пути)

```
<корень_данных>/
├── inbox/                    # Рабочий каталог: пользователь кладёт PDF сюда
├── processing/               # Файлы в процессе OCR
└── documents/
    └── <имя_документа>/      # Подкаталог на документ (имя = имя PDF или задано пользователем)
        ├── source.pdf        # Исходный скан (копия или перемещение — настройка)
        ├── document.pdf      # Итоговый PDF/A с текстовым слоем
        ├── document.md       # Полный распознанный текст в Markdown
        ├── metadata.json     # Метаданные: даты, модель, страницы, хеш
        └── pages/            # Опционально: PNG страниц, постраничный MD
            ├── page_001.md
            └── page_001.png
```

### 4.2. Векторная база данных

```
<корень_данных>/vectorstore/
├── chroma/                   # или qdrant_local/
├── collections/
│   └── documents/            # Коллекция по умолчанию
└── registry.json             # Реестр: document_id ↔ путь ↔ chunk_ids
```

### 4.3. Метаданные чанка (обязательные поля)

```json
{
  "document_id": "uuid",
  "document_name": "Договор_2024",
  "file_path": "C:/.../documents/Договор_2024/document.pdf",
  "md_path": "C:/.../documents/Договор_2024/document.md",
  "chunk_index": 12,
  "page_start": 5,
  "page_end": 6,
  "char_start": 10240,
  "char_end": 11240,
  "created_at": "2026-06-16T12:00:00",
  "embedding_model": "Qwen3-Embedding-8B",
  "ocr_model": "Qwen3-VL-30B-A3B-Instruct"
}
```

---

## 5. Модуль 1 — Обработка PDF

### 5.1. Назначение

Автоматическое обнаружение новых сканированных PDF, постраничное OCR, создание PDF/A с поиском, Markdown-дамп, векторизация и размещение в каталоге `documents`.

### 5.2. Пользовательский сценарий

1. Пользователь указывает **рабочий каталог** (`inbox`) в настройках модуля.
2. Пользователь копирует/перемещает PDF в `inbox`.
3. Модуль обнаруживает файл → перемещает в `processing` → ставит задачу в очередь.
4. Для каждой страницы: рендер в изображение → запрос к OCR API → накопление текста.
5. Формируется `document.md` (конкатенация постраничного текста с разметкой `# Страница N`).
6. Создаётся PDF/A: изображения страниц + невидимый/прозрачный текстовый слой для поиска.
7. При наличии блока «Содержание» в OCR-результате — встраивается **PDF Outline (закладки)**.
8. Выполняется чанкинг MD → batch embedding → запись в векторную БД.
9. Результат перемещается в `documents/<имя>/`.
10. Статус в UI: прогресс-бар, лог, список обработанных файлов.

### 5.3. Детальный pipeline обработки

```
PDF в inbox
    → валидация (формат, не пароль, не повреждён)
    → перемещение в processing
    → [опционально] предобработка изображений (коррекция)
    → для каждой страницы i..N:
          render(page_i) → image
          OCR API(image, page=i) → text_i, structure_i
          append to document.md
          build text layer for page_i
    → merge text layers + images → PDF
    → конвертация в PDF/A-2b (настройка уровня: 1b/2b/3)
  → chunk(document.md) → embed API → vectorstore.add
    → запись metadata.json
    → перемещение в documents/<name>/
    → удаление из processing (или архив — настройка)
```

### 5.4. Предобработка сканов (коррекция)

Настраиваемые опции (применяются до OCR):

| Настройка | Описание | По умолчанию |
|-----------|----------|--------------|
| `deskew` | Выравнивание наклона страницы | Вкл |
| `denoise` | Шумоподавление | Выкл |
| `contrast_enhance` | Повышение контраста | Выкл |
| `binarize` | Бинаризация (Otsu/adaptive) | Выкл |
| `dpi` | DPI рендера страницы (150–600) | 300 |
| `crop_margins` | Обрезка полей (мм) | 0 |

### 5.5. OCR-настройки

| Настройка | Tooltip-описание (кратко) | Значение по умолчанию |
|-----------|---------------------------|----------------------|
| `ocr.api.host` | IP-адрес ПК с Qwen3-VL в локальной сети | — |
| `ocr.api.port` | Порт HTTP API | 8000 |
| `ocr.api.timeout` | Макс. время ожидания ответа на одну страницу (сек) | 120 |
| `ocr.api.retries` | Повторы при ошибке сети | 3 |
| `ocr.language` | Подсказка языка для модели (`ru`, `en`, `auto`) | `auto` |
| `ocr.prompt` | Промпт: извлечь текст, заголовки, содержание | Шаблон из конфига |
| `ocr.page_parallel` | Параллельных страниц (осторожно с VRAM на сервере) | 1 |
| `ocr.output_format` | Формат страницы в MD (`plain`, `structured`) | `structured` |
| `ocr.detect_toc` | Искать и парсить содержание | Вкл |

### 5.6. Настройки PDF/A

| Настройка | Описание | По умолчанию |
|-----------|----------|--------------|
| `pdfa.level` | Уровень PDF/A: `2b` рекомендуется | `2b` |
| `pdfa.embed_fonts` | Встраивание шрифтов | Вкл |
| `pdfa.pdf_version` | Версия PDF | 1.7 |
| `pdfa.title`, `author` | Метаданные документа | Из имени файла |
| `pdfa.create_outline` | Создавать закладки из содержания | Вкл |
| `pdfa.outline_levels` | Макс. уровень заголовков в outline | 3 |
| `pdfa.text_opacity` | Прозрачность текстового слоя (0 = невидим) | 0 |
| `pdfa.keep_original` | Сохранять копию исходника в папке документа | Вкл |

### 5.7. Настройки векторизации (в контексте модуля 1)

| Настройка | Описание | По умолчанию |
|-----------|----------|--------------|
| `embed.api.host` / `port` | Подключение к Qwen3-Embedding-8B | — |
| `chunk.size` | Размер чанка в символах | 1000 |
| `chunk.overlap` | Перекрытие чанков | 200 |
| `chunk.strategy` | `fixed`, `paragraph`, `heading` | `paragraph` |
| `chunk.min_size` | Мин. размер чанка | 100 |
| `embed.batch_size` | Чанков за один API-запрос | 16 |
| `embed.normalize` | L2-нормализация | Вкл |

### 5.8. UI модуля 1

**Макет (рекомендуемый):**

- Левая панель: настройки каталогов, кнопки «Открыть inbox», «Запустить/Остановить мониторинг».
- Центр: таблица задач (файл, статус, прогресс %, страница N/M, ошибки).
- Правая панель: вкладки настроек (OCR, PDF, PDF/A, Embedding, Предобработка).
- Верхняя строка состояния: индикаторы **OCR API** | **Embedding API**.
- Нижняя панель: лог обработки (фильтр по уровню).

**Диалог переименования:** перед финальным перемещением в `documents` пользователь может изменить имя подкаталога (по умолчанию — имя PDF без расширения).

### 5.9. Обработка ошибок

| Ситуация | Поведение |
|----------|-----------|
| OCR недоступен | Задача в статусе «Ошибка», файл остаётся в `processing`, уведомление |
| Ошибка на странице K | Повтор до `retries`; при неудаче — пометка страницы в MD как `[OCR ERROR page K]` |
| Embedding недоступен | PDF/MD сохраняются; векторизация в очередь «отложенных» |
| Дубликат документа | Предупреждение; опции: перезаписать, версионировать (`_v2`), пропустить |

---

## 6. Модуль 2 — Управление RAG-архитектурой

### 6.1. Назначение

Администрирование векторной базы, метаданных, файловой структуры документов; пере-embedding; синхронизация файловой системы и векторного индекса.

### 6.2. Функциональные требования

#### 6.2.1. Просмотр и редактирование метаданных

- Таблица документов: имя, путь, число чанков, дата индексации, модель embedding.
- Редактирование полей метаданных документа и отдельных чанков (через форму).
- Массовое редактирование тегов (опциональное поле `tags[]` в метаданных).

#### 6.2.2. Пере-embedding (re-embed)

- Выбор документов или всей коллекции.
- Параметры: новая модель (если сменился endpoint), размер чанка, overlap.
- Прогресс: документ / чанк / ETA.
- Атомарность: при сбое — откат к предыдущей версии индекса документа (snapshot).

#### 6.2.3. Файловая структура документов

- Древовидный проводник по `documents/` с операциями:
  - переименование папки/файла → обновление путей в метаданных и vectorstore;
  - удаление документа → удаление чанков из БД;
  - добавление внешнего MD/PDF → опциональная индексация без OCR.
- **Синхронизация:** кнопка «Проверить целостность» — сравнение registry.json, ФС и vectorstore; отчёт об осиротевших векторах и отсутствующих файлах.

#### 6.2.4. Управление коллекциями RAG

- Создание/удаление коллекций (для разных проектов).
- Экспорт/импорт метаданных (JSON).
- Статистика: общее число векторов, размер на диске, средняя длина чанка.

### 6.3. UI модуля 2

- Вкладка «Документы» — таблица + детали.
- Вкладка «Чанки» — просмотр текста чанка, соседние чанки, вектор (опционально t-SNE не требуется).
- Вкладка «Файлы» — TreeView с контекстным меню.
- Вкладка «Синхронизация» — отчёты, кнопки исправления.
- Вкладка «Настройки RAG» — chunking, embedding API, путь к vectorstore.
- Индикатор **Embedding API** и **LLM API** (если используется для тестовых запросов).

### 6.4. Настройки модуля 2 (с tooltips)

| Элемент | Tooltip |
|---------|---------|
| Путь к vectorstore | Локальная папка ChromaDB/Qdrant. Не перемещайте во время индексации. Влияет на все модули поиска. |
| Коллекция по умолчанию | Имя коллекции для новых документов. Рекомендуется одна коллекция для одного архива. |
| Стратегия чанкинга | `paragraph` лучше для договоров; `fixed` — для однородного текста. Влияет на точность RAG. |
| Re-embed batch | Увеличивайте при стабильной сети и мощном сервере embedding. Снижайте при OOM на сервере. |
| Автосинхронизация путей | При переименовании в проводнике Windows вне приложения — предлагать ручную синхронизацию. |

---

## 7. Модуль 3 — Поисковый агент

### 7.1. Назначение

Основной рабочий интерфейс: чат с LLM на основе RAG, потоковые ответы, ссылки на файлы-источники, быстрый поиск по имени файла.

### 7.2. Подключения (настройки модуля)

| Ресурс | Настройка |
|--------|-----------|
| Каталог документов | Путь к `documents/` |
| Векторная БД | Путь / имя коллекции |
| Embedding API | Для векторизации запроса (тот же Qwen3-Embedding-8B) |
| Search LLM API | IP, port, модель, streaming |

### 7.3. Сценарий поиска (RAG + LLM)

```
Запрос пользователя
    → [опционально] query expansion / переформулировка (настройка)
    → embedding(query) через LAN API
    → vectorstore.search(top_k=5, filters)
    → формирование контекста из 5 ближайших чанков
    → prompt = system + context + user query
    → LLM API (stream=true)
    → постепенный вывод в окно чата
    → парсинг ответа: вставка кликабельных ссылок на файлы
```

### 7.4. Требования к ответу агента

1. **Top-5 кандидатов** — в контекст LLM передаются ровно 5 наиболее релевантных чанков (настраиваемо `search.top_k`, default=5).
2. **Ссылки на файлы** — в ответе блок «Источники» со списком:
   - имя документа (клик → открыть PDF в системе по умолчанию);
   - путь к MD (клик → открыть в блокноте/ассоциированном редакторе);
   - номера страниц (клик → открыть PDF на странице, если ридер поддерживает).
3. **Цитирование** — в system prompt зафиксировать требование указывать `[Документ, стр. N]` при использовании факта из контекста.
4. **Streaming** — токены отображаются в реальном времени в **окне ответа над полем ввода**.

### 7.5. Файловый поиск по имени

- Отдельная панель или строка поиска «Найти файл…».
- Быстрый фильтр по имени папки/файла в `documents/` (substring, без учёта регистра).
- Результаты: список с двойным кликом → открытие в проводнике / PDF.
- Не заменяет семантический поиск; работает локально без API.

### 7.6. UI модуля 3 (макет)

```
┌────────────────────────────────────────────────────────────┐
│  [●] Embedding   [●] Search LLM          Настройки поиска │
├──────────────────────────┬─────────────────────────────────┤
│  Файловый поиск          │                                 │
│  [___________] [Найти]   │     Окно истории чата           │
│  - Договор_2024          │     (streaming ответ LLM)       │
│  - Акт_приёма            │                                 │
│                          │                                 │
├──────────────────────────┴─────────────────────────────────┤
│  [ Поле ввода запроса........................... ] [Отпр.]  │
└────────────────────────────────────────────────────────────┘
```

- История чатов: сессии с сохранением в локальную SQLite (опционально).
- Кнопка «Очистить контекст» — новая сессия без истории сообщений.

### 7.7. Настройки поиска

| Настройка | Описание | Default |
|-----------|----------|---------|
| `search.top_k` | Число чанков в контексте | 5 |
| `search.score_threshold` | Мин. косинусная близость | 0.3 |
| `search.mmr` | MMR diversification | Выкл |
| `search.mmr_lambda` | Баланс релевантность/разнообразие | 0.5 |
| `search.max_context_tokens` | Лимит токенов контекста | 8000 |
| `search.filters` | JSON-фильтр по метаданным | — |
| `llm.temperature` | Креативность ответа | 0.2 |
| `llm.max_tokens` | Макс. длина ответа | 2048 |
| `llm.stream` | Потоковый вывод | Вкл |
| `llm.system_prompt` | Редактируемый системный промпт | Шаблон |
| `search.hybrid` | Гибрид: вектор + BM25 по MD (опционально фаза 2) | Выкл |

### 7.8. Tooltips для модуля 3 (обязательные)

Каждый элемент управления должен иметь tooltip формата:

> **Что это:** …  
> **Зачем:** …  
> **Рекомендация:** …  
> **Влияние:** …

Пример для `search.top_k`:

> **Что это:** Количество фрагментов текста из базы, передаваемых LLM.  
> **Зачем:** Определяет объём контекста для ответа.  
> **Рекомендация:** 5 — баланс точности и скорости; увеличьте для сложных вопросов.  
> **Влияние:** Больше K → длиннее промпт, выше расход токенов, возможны противоречивые фрагменты.

---

## 8. Общие требования к GUI/UX

### 8.1. Внешний вид

- Современный стиль: тёмная и светлая тема (переключатель в настройках).
- Единая дизайн-система: отступы 8px grid, скругление 6–8px, акцентный цвет настраиваемый.
- Шрифты: **Segoe UI** (системный Windows), моноширинный для логов — **Cascadia Code**.
- Иконки: **Material Symbols** или **Fluent UI**-совместимый набор.
- Адаптивные панели: возможность изменять ширину split-панелей.

### 8.2. Автосохранение геометрии окон

**Обязательно** для главного окна и всех дочерних окон/диалогов:

- При изменении размера или позиции окна — сохранение в `config/ui_state.json`.
- При следующем запуске — восстановление размеров, позиции, состояния maximized.
- Сохранение ширины колонок таблиц и позиций splitters.

### 8.3. Tooltips

- Задержка появления: 500 мс.
- Многострочные tooltips для сложных настроек.
- Ссылка «Подробнее» в tooltip → открытие встроенной справки (опционально).

### 8.4. Главное окно приложения

- Навигация: боковая панель или вкладки: **Обработка PDF** | **RAG** | **Поиск**.
- Меню: Файл, Настройки, Справка, О программе.
- Глобальные настройки: пути, тема, язык UI (русский по умолчанию).

### 8.5. Доступность

- Клавиатурная навигация по основным элементам.
- Контраст не ниже WCAG AA для текста.
- Масштабирование UI: 100% / 125% / 150%.

---

## 9. Конфигурация приложения

### 9.1. Файл настроек

Путь: `%APPDATA%/Pdf_scan_and_ocr/settings.yaml`

Структура верхнего уровня:

```yaml
app:
  language: ru
  theme: dark
  data_root: D:/PdfArchive

modules:
  pdf_processor: { ... }
  rag_manager: { ... }
  search_agent: { ... }

api:
  ocr: { host, port, timeout, ... }
  embedding: { host, port, ... }
  llm: { host, port, stream, ... }

ui_state:
  main_window: { x, y, width, height, maximized }
```

### 9.2. Профили API

Возможность сохранить несколько профилей подключения (например, «Домашний сервер», «Офис») с быстрым переключением.

### 9.3. Секреты

API-ключи (если потребуются) хранить в Windows Credential Manager или локальном зашифрованном файле, **не** в открытом YAML.

---

## 10. Нефункциональные требования

| Требование | Значение |
|------------|----------|
| ОС | Windows 10 (1903+), Windows 11 |
| RAM клиента | Мин. 8 ГБ (рекомендуется 16 ГБ) |
| Диск | Зависит от объёма архива; vectorstore ~1–2 КБ на чанк |
| Сеть | LAN 1 Гбит рекомендуется для OCR |
| Одновременная обработка | Настраиваемое число воркеров (default: 1 PDF) |
| Логи | Ротация 10 МБ × 5 файлов |
| Локализация | UI на русском; OCR/LLM промпты настраиваемые |
| Установка | Инсталлятор MSI или portable ZIP |
| Обновления | Проверка версии на GitHub Releases (опционально) |

---

## 11. Безопасность

1. Все данные по умолчанию остаются на ПК пользователя.
2. API в LAN — без передачи в публичный интернет (настраиваемый `allow_external_hosts=false`).
3. Валидация путей: запрет записи вне `data_root` без подтверждения.
4. Логи не содержат полный текст документов (только идентификаторы и номера страниц).

---

## 12. Этапы разработки

### Этап 1 — Core + Модуль 1 (MVP)

- [ ] Каркас приложения, конфигурация, автосохранение окон
- [ ] File watcher, очередь задач
- [ ] Клиент OCR API, постраничная обработка
- [ ] Генерация MD и PDF с текстовым слоем
- [ ] Базовая конвертация PDF/A
- [ ] Индикаторы API

### Этап 2 — RAG

- [ ] Chunking, клиент Embedding API
- [ ] Интеграция ChromaDB/Qdrant
- [ ] Модуль 2: просмотр, синхронизация, re-embed

### Этап 3 — Поисковый агент

- [ ] Модуль 3: чат, streaming, top-5 RAG
- [ ] Ссылки на файлы, файловый поиск
- [ ] Полный набор tooltips

### Этап 4 — Полировка

- [ ] PDF Outline из содержания
- [ ] Темы, справка, инсталлятор
- [ ] Тесты, документация пользователя

---

## 13. Критерии приёмки

### Модуль 1

1. PDF, помещённый в `inbox`, автоматически обрабатывается без ручного запуска (при включённом мониторинге).
2. Итоговый файл в `documents/<имя>/` является PDF/A с возможностью поиска текста в Adobe Reader / Edge.
3. Файл `document.md` содержит полный распознанный текст всех страниц.
4. Векторы записаны в БД; поиск по чанкам возвращает результаты.
5. Индикаторы OCR и Embedding отражают реальное состояние API.

### Модуль 2

1. Переименование папки документа обновляет пути в метаданных и vectorstore.
2. Re-embed выбранного документа завершается без потери других документов.
3. Отчёт синхронизации выявляет рассогласования ФС и БД.

### Модуль 3

1. Запрос в чате вызывает потоковый ответ в окне над полем ввода.
2. В ответе присутствуют ссылки минимум на один файл-источник из top-5.
3. Файловый поиск находит документ по части имени и открывает его по двойному клику.
4. Все настройки модуля имеют информативные tooltips.

### Общие

1. Приложение запускается на Windows 10 и 11 без установленного Python (после сборки).
2. Размеры и позиция главного окна восстанавливаются после перезапуска.

---

## 14. Риски и ограничения

| Риск | Митигация |
|------|-----------|
| Qwen3-VL 30B требует мощный GPU на сервере | Параметр `page_parallel=1`, очередь задач |
| Нестандартный API на сервере | Слой адаптеров + документация формата запроса |
| Большие PDF (500+ стр.) | Разбиение на батчи, возобновление с последней страницы |
| Качество OCR на плохих сканах | Блок предобработки, ручная повторная обработка страницы |
| PDF/A строгая валидация | Опциональная верификация через veraPDF (внешний инструмент) |

---

## 15. Глоссарий

| Термин | Определение |
|--------|-------------|
| **PDF/A** | Стандарт ISO для долгосрочного архивного хранения PDF |
| **OCR** | Optical Character Recognition — распознавание текста на изображении |
| **RAG** | Retrieval-Augmented Generation — поиск релевантного контекста + генерация ответа LLM |
| **Чанк** | Фрагмент текста фиксированного размера для векторизации |
| **Embedding** | Векторное представление текста в многомерном пространстве |
| **Streaming** | Потоковая передача ответа LLM по мере генерации |
| **Outline** | Дерево закладок (содержание) внутри PDF |

---

## 16. Приложения

### Приложение А — Шаблон system prompt для поискового агента

```
Ты — ассистент по поиску в архиве отсканированных документов.
Отвечай только на основе предоставленного контекста.
Если информации недостаточно — честно сообщи об этом.
При каждом факте указывай источник: [ИмяДокумента, стр. N].
В конце ответа перечисли все использованные документы со путями для открытия.
Контекст:
{context}
```

### Приложение Б — Шаблон OCR prompt

```
Распознай весь текст на изображении страницы документа.
Сохрани структуру: заголовки (уровни 1-3), абзацы, списки, таблицы в markdown.
Если на странице есть блок «Содержание» — выдели его отдельной секцией ## Содержание.
Верни только текст страницы без пояснений.
Язык: {language}
```

### Приложение В — Минимальные версии зависимостей (ориентир)

```
Python>=3.11
PySide6>=6.6
watchdog>=4.0
PyMuPDF>=1.24
pikepdf>=9.0
chromadb>=0.5
httpx>=0.27
pydantic-settings>=2.0
loguru>=0.7
```

---

**Конец документа**
