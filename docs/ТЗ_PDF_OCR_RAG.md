# Техническое задание (ТЗ)
# Проект: Pdf_scan_and_ocr — конвертация сканированных PDF в PDF/A с семантическим поиском

| Поле | Значение |
|------|----------|
| **Версия документа** | 1.1 |
| **Дата** | 16.06.2026 |
| **Платформа** | Windows 10 / Windows 11 |
| **Язык разработки** | Python 3.11+ |
| **Тип приложения** | Desktop GUI, модульная архитектура |
| **Репозиторий** | Pdf_scan_and_ocr |

---

## 1. Назначение и цели проекта

### 1.1. Назначение

Разработать настольное GUI-приложение для автоматизированной обработки отсканированных PDF-документов: постраничное распознавание текста с помощью LLM OCR, формирование поисковых PDF/A с автоматическим извлечением номера и наименования документа, векторизация содержимого для RAG-базы и семантический поиск по документам в табличном виде (без LLM-агента).

### 1.2. Цели

1. Преобразование сканированных PDF в **PDF/A** с **встроенным текстовым слоем** (полнотекстовый поиск в PDF-ридере).
2. Сохранение полного распознанного текста в **Markdown** для последующей обработки и RAG.
3. Построение **векторной базы знаний** (RAG) на основе содержимого документов.
4. Предоставление **поискового модуля** для семантического поиска: один запрос — одна таблица результатов с номером документа, наименованием, краткой вырезкой текста и ссылкой на PDF/A.
5. **Подтверждение пользователем** автоматически извлечённых номера и наименования документа после обработки (подтвердить как есть или отредактировать и подтвердить).
6. Модульное разделение функций: обработка, управление RAG, поиск.
7. Работа с LLM-моделями (OCR, embedding), развёрнутыми **на отдельных ПК в локальной сети** (доступ по IP и порту).

### 1.3. Целевая аудитория

Пользователи, работающие с архивами отсканированных документов (делопроизводство, юридические отделы, библиотеки, личные архивы), которым необходимы структурированный поиск по содержимому, контроль метаданных документов (номер, наименование) и быстрый доступ к исходным PDF/A.

---

## 2. Общая архитектура

### 2.1. Принципы архитектуры

| Принцип | Описание |
|---------|----------|
| **Модульность** | Три независимых функциональных модуля с общим ядром (core) |
| **Слабая связанность** | Модули взаимодействуют через общие сервисы, конфигурацию и файловую структуру |
| **Расширяемость** | Возможность добавления новых моделей, провайдеров API, форматов экспорта |
| **Локальность данных** | Все документы и векторная БД хранятся локально на ПК пользователя |
| **Сетевые LLM** | OCR и embedding — внешние сервисы в LAN по HTTP API; поисковый модуль использует только embedding |

### 2.2. Высокоуровневая схема

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        GUI Application (Windows)                       │
├──────────────┬──────────────────────┬───────────────────────────────────┤
│  Модуль 1    │      Модуль 2        │         Модуль 3                  │
│  Обработка   │  Управление RAG      │    Поисковый модуль               │
│  PDF         │                      │    (семантический поиск)          │
└──────┬───────┴──────────┬───────────┴──────────────┬──────────────────┘
       │                  │                            │
       ▼                  ▼                            ▼
┌──────────────────────────────────────────────────────────────────────────┐
│                         Core Services Layer                               │
│  ConfigManager │ FileWatcher │ JobQueue │ VectorStore │ DocumentRegistry │
└──────┬─────────────────┬──────────────────────┬──────────────────────────┘
       │                 │                      │
       ▼                 ▼                      ▼
┌─────────────┐  ┌──────────────┐
│  LAN API    │  │  LAN API     │
│  Qwen3-VL   │  │  Qwen3-      │
│  30B OCR    │  │  Embedding   │
│  IP:Port    │  │  8B          │
│             │  │  IP:Port     │
└─────────────┘  └──────────────┘
```

### 2.3. Рекомендуемый технологический стек

| Компонент | Технология | Обоснование |
|-----------|------------|-------------|
| GUI-фреймворк | **PySide6** (Qt6) или **CustomTkinter** | Нативный вид на Windows, богатые виджеты, поддержка tooltips |
| Асинхронность | **asyncio** + **QThread** / **threading** | Неблокирующий UI при длительной OCR и embedding |
| Мониторинг каталогов | **watchdog** | Отслеживание появления PDF в рабочем каталоге |
| PDF-обработка | **PyMuPDF (fitz)**, **pikepdf**, **ocrmypdf** (опционально) | Рендер страниц, встраивание текста, PDF/A |
| PDF/A конвертация | **pikepdf** + **Ghostscript** (внешняя зависимость) | Соответствие стандарту PDF/A |
| Векторная БД | **ChromaDB** или **Qdrant** (локальный режим) | Локальное хранение, метаданные, фильтрация |
| HTTP-клиент | **httpx** / **aiohttp** | Асинхронные запросы к LAN API |
| Конфигурация | **pydantic-settings** + **YAML/JSON** | Валидация настроек, профили подключения |
| Логирование | **loguru** | Ротация логов, уровни детализации |
| Упаковка | **PyInstaller** / **cx_Freeze** | Дистрибутив для Windows 10/11 |

### 2.4. Структура репозитория (целевая)

```
Pdf_scan_and_ocr/
├── docs/
│   └── ТЗ_PDF_OCR_RAG.md          # Настоящий документ
├── src/
│   ├── main.py                     # Точка входа
│   ├── app/
│   │   ├── application.py          # QApplication, главное окно
│   │   └── styles/                 # Темы, QSS
│   ├── core/
│   │   ├── config/                 # Менеджер настроек
│   │   ├── api/                    # Клиенты LAN API (OCR, embed)
│   │   ├── pdf/                    # Рендер, PDF/A, TOC
│   │   ├── rag/                    # Chunking, vectorstore, metadata
│   │   └── jobs/                   # Очередь задач, воркеры
│   ├── modules/
│   │   ├── pdf_processor/          # Модуль 1
│   │   ├── rag_manager/            # Модуль 2
│   │   └── search/               # Модуль 3 — семантический поиск
│   └── shared/
│       ├── widgets/                # Общие виджеты (индикатор API, tooltips)
│       └── utils/
├── data/                           # Рабочие каталоги (настраиваемые)
│   ├── inbox/                      # Входящие PDF
│   ├── processing/                 # В обработке
│   └── documents/                  # Готовые документы
├── config/
│   └── default_settings.yaml
├── tests/
├── requirements.txt
└── README.md
```

---

## 3. Внешние LLM-сервисы (LAN API)

### 3.1. OCR — Qwen3-VL-30B-A3B-Instruct

- **Модель:** [Qwen/Qwen3-VL-30B-A3B-Instruct](https://huggingface.co/Qwen/Qwen3-VL-30B-A3B-Instruct)
- **Назначение:** Постраничное распознавание текста, определение структуры (заголовки, содержание, таблицы).
- **Развёртывание:** Отдельный ПК в LAN; приложение подключается по `http://<IP>:<PORT>`.
- **Ожидаемый интерфейс API** (адаптер в приложении должен поддерживать OpenAI-compatible и кастомный эндпоинт):

| Параметр | Описание |
|----------|----------|
| `base_url` | Базовый URL сервиса |
| `endpoint` | `/v1/chat/completions` или кастомный `/ocr/recognize` |
| `timeout_sec` | Таймаут запроса (по умолчанию 120 с на страницу) |
| `max_retries` | Число повторов при сетевой ошибке |
| `image_format` | PNG/JPEG, DPI рендера страницы |
| `prompt_template` | Настраиваемый промпт для OCR |

**Входные данные на страницу:** изображение страницы (base64 или multipart), номер страницы, язык (опционально).

**Выходные данные:** распознанный текст (plain/markdown), метаданные структуры (заголовки H1–H3, блок «Содержание»), уровень уверенности (если API отдаёт).

### 3.2. Embedding — Qwen3-Embedding-8B

- **Модель:** [Qwen/Qwen3-Embedding-8B](https://huggingface.co/Qwen/Qwen3-Embedding-8B)
- **Назначение:** Векторизация чанков текста для RAG.
- **Развёртывание:** Отдельный ПК в LAN, `http://<IP>:<PORT>`.

| Параметр | Описание |
|----------|----------|
| `base_url` | Базовый URL |
| `endpoint` | `/v1/embeddings` или `/embed` |
| `batch_size` | Размер пакета чанков (1–64) |
| `dimensions` | Размерность вектора (если модель поддерживает truncate) |
| `normalize` | L2-нормализация векторов |

### 3.3. Использование Embedding в поисковом модуле

Модуль 3 **не использует LLM-агента**. Для семантического поиска применяется только модель **Qwen3-Embedding-8B** (тот же сервис, что и при индексации в модулях 1 и 2):

- **Векторизация запроса:** пользовательский текст → embedding через LAN API.
- **Поиск:** `vectorstore.search()` по коллекции документов с сортировкой по релевантности (косинусная близость).
- **Формирование результата:** для каждого найденного чанка извлекаются метаданные документа и текстовая вырезка; LLM для генерации ответов **не вызывается**.

| Параметр | Описание |
|----------|----------|
| `base_url`, `endpoint` | Те же, что для embedding при индексации |
| `normalize` | L2-нормализация вектора запроса (согласованно с индексацией) |
| `timeout_sec` | Таймаут запроса embedding (по умолчанию 30 с) |

### 3.4. Индикатор подключения к API

Для каждого внешнего сервиса (OCR, Embedding) в GUI отображается:

| Состояние | Визуализация | Поведение |
|-----------|--------------|-----------|
| **Подключено** | Зелёный индикатор | Успешный health-check / тестовый запрос < 5 с назад |
| **Проверка** | Жёлтый, анимация | Идёт ping или запрос |
| **Отключено** | Красный | Ошибка сети, таймаут, HTTP 4xx/5xx |
| **Не настроено** | Серый | IP/порт не заданы |

Периодическая проверка: каждые **30 с** (настраиваемо). Ручная кнопка «Проверить подключение» на панели каждого модуля.

---

## 4. Файловая структура данных

### 4.1. Каталоги модуля обработки PDF (настраиваемые пути)

```
<корень_данных>/
├── inbox/                    # Рабочий каталог: пользователь кладёт PDF сюда
├── processing/               # Файлы в процессе OCR
└── documents/
    └── <имя_документа>/      # Подкаталог на документ (имя = имя PDF или задано пользователем)
        ├── source.pdf        # Исходный скан (копия или перемещение — настройка)
        ├── document.pdf      # Итоговый PDF/A с текстовым слоем
        ├── document.md       # Полный распознанный текст в Markdown
        ├── metadata.json     # Метаданные: номер, наименование, даты, модель, страницы, хеш, статус подтверждения
        └── pages/            # Опционально: PNG страниц, постраничный MD
            ├── page_001.md
            └── page_001.png
```

### 4.2. Векторная база данных

```
<корень_данных>/vectorstore/
├── chroma/                   # или qdrant_local/
├── collections/
│   └── documents/            # Коллекция по умолчанию
└── registry.json             # Реестр: document_id ↔ путь ↔ chunk_ids
```

### 4.3. Метаданные документа (обязательные поля)

Поля `document_number` и `document_name` извлекаются **автоматически** при обработке PDF (модуль 1) и подлежат **подтверждению пользователем** перед окончательной индексацией в RAG (см. разделы 5 и 6).

```json
{
  "document_id": "uuid",
  "document_number": "12/34-2024",
  "document_name": "Договор поставки оборудования",
  "document_number_source": "auto",
  "document_name_source": "auto",
  "metadata_confirmed": false,
  "metadata_confirmed_at": null,
  "file_path": "C:/.../documents/Договор_2024/document.pdf",
  "md_path": "C:/.../documents/Договор_2024/document.md",
  "created_at": "2026-06-16T12:00:00",
  "ocr_model": "Qwen3-VL-30B-A3B-Instruct",
  "embedding_model": "Qwen3-Embedding-8B",
  "pages_count": 24
}
```

| Поле | Описание |
|------|----------|
| `document_number` | Регистрационный/внутренний номер документа (извлекается из текста первых страниц, штампа, реквизитов) |
| `document_name` | Наименование/заголовок документа (извлекается из заголовка, темы письма, названия в тексте) |
| `document_number_source` / `document_name_source` | `auto` — сформировано автоматикой; `user` — скорректировано пользователем |
| `metadata_confirmed` | `false` — ожидает подтверждения; `true` — пользователь подтвердил номер и наименование |
| `metadata_confirmed_at` | Дата и время подтверждения (ISO 8601) |

Документы с `metadata_confirmed: false` **не участвуют** в поиске модуля 3 (фильтр по умолчанию).

### 4.4. Метаданные чанка (обязательные поля)

```json
{
  "document_id": "uuid",
  "document_number": "12/34-2024",
  "document_name": "Договор поставки оборудования",
  "file_path": "C:/.../documents/Договор_2024/document.pdf",
  "md_path": "C:/.../documents/Договор_2024/document.md",
  "chunk_index": 12,
  "chunk_text": "фрагмент текста чанка для вырезки в результатах поиска",
  "page_start": 5,
  "page_end": 6,
  "char_start": 10240,
  "char_end": 11240,
  "created_at": "2026-06-16T12:00:00",
  "embedding_model": "Qwen3-Embedding-8B",
  "ocr_model": "Qwen3-VL-30B-A3B-Instruct"
}
```

---

## 5. Модуль 1 — Обработка PDF

### 5.1. Назначение

Автоматическое обнаружение новых сканированных PDF, постраничное OCR, **автоматическое извлечение номера и наименования документа**, создание PDF/A с поиском, Markdown-дамп, **подтверждение метаданных пользователем**, векторизация и размещение в каталоге `documents`.

### 5.2. Пользовательский сценарий

1. Пользователь указывает **рабочий каталог** (`inbox`) в настройках модуля.
2. Пользователь копирует/перемещает PDF в `inbox`.
3. Модуль обнаруживает файл → перемещает в `processing` → ставит задачу в очередь.
4. Для каждой страницы: рендер в изображение → запрос к OCR API → накопление текста.
5. Формируется `document.md` (конкатенация постраничного текста с разметкой `# Страница N`).
6. Создаётся PDF/A: изображения страниц + невидимый/прозрачный текстовый слой для поиска.
7. При наличии блока «Содержание» в OCR-результате — встраивается **PDF Outline (закладки)**.
8. **Автоматическое извлечение метаданных:** из распознанного текста (первые 1–3 страницы, штампы, заголовки) формируются поля `document_number` и `document_name`; записываются в `metadata.json` с `metadata_confirmed: false`.
9. Пользователю показывается **диалог подтверждения метаданных** (номер и наименование): подтвердить как есть или отредактировать и подтвердить.
10. После подтверждения (`metadata_confirmed: true`) выполняется чанкинг MD → batch embedding → запись в векторную БД.
11. Результат перемещается в `documents/<имя>/`.
12. Статус в UI: прогресс-бар, лог, список обработанных файлов; отдельный статус «Ожидает подтверждения метаданных».

### 5.3. Детальный pipeline обработки

```
PDF в inbox
    → валидация (формат, не пароль, не повреждён)
    → перемещение в processing
    → [опционально] предобработка изображений (коррекция)
    → для каждой страницы i..N:
          render(page_i) → image
          OCR API(image, page=i) → text_i, structure_i
          append to document.md
          build text layer for page_i
    → merge text layers + images → PDF
    → конвертация в PDF/A-2b (настройка уровня: 1b/2b/3)
    → extract_metadata(document.md) → document_number, document_name
    → запись metadata.json (metadata_confirmed=false)
    → [UI] диалог подтверждения номера и наименования пользователем
    → metadata_confirmed=true, обновление vectorstore metadata
    → chunk(document.md) → embed API → vectorstore.add
    → перемещение в documents/<name>/
    → удаление из processing (или архив — настройка)
```

### 5.4. Извлечение номера и наименования документа

После OCR и формирования `document.md` модуль 1 автоматически определяет:

| Поле | Источники извлечения (приоритет сверху вниз) |
|------|-----------------------------------------------|
| `document_number` | Штамп/реквизит «№», «Исх. №», «Вх. №» на первой странице; паттерны датированных номеров; метаданные PDF (если есть) |
| `document_name` | Заголовок H1/H2 на первой странице; строка «Тема:» / «Наименование:»; имя исходного PDF (fallback) |

- Алгоритм: эвристики + опциональный запрос к OCR-модели с узким промптом «извлеки номер и наименование» (настройка `metadata.extract_via_llm`, по умолчанию Вкл).
- При неудачном извлечении поля заполняются плейсхолдерами (`—`) — пользователь обязан ввести значения в диалоге подтверждения.
- До подтверждения векторизация **откладывается** (документ в статусе `pending_metadata`).

### 5.5. Диалог подтверждения метаданных (модуль 1)

После завершения OCR и создания PDF/A пользователю отображается модальное окно:

| Элемент | Описание |
|---------|----------|
| Поле «Номер документа» | Предзаполнено автоматически извлечённым значением; редактируемое |
| Поле «Наименование документа» | Предзаполнено автоматически извлеченным значением; редактируемое |
| Превью | Миниатюра первой страницы или фрагмент текста-источника извлечения |
| Кнопка «Подтвердить» | Сохраняет значения, `metadata_confirmed=true`, запускает embedding |
| Кнопка «Отложить» | Документ остаётся в `pending_metadata`; подтверждение возможно позже в модуле 2 |

Повторное открытие диалога — из таблицы задач (контекстное меню «Подтвердить метаданные»).

### 5.6. Предобработка сканов (коррекция)

Настраиваемые опции (применяются до OCR):

| Настройка | Описание | По умолчанию |
|-----------|----------|--------------|
| `deskew` | Выравнивание наклона страницы | Вкл |
| `denoise` | Шумоподавление | Выкл |
| `contrast_enhance` | Повышение контраста | Выкл |
| `binarize` | Бинаризация (Otsu/adaptive) | Выкл |
| `dpi` | DPI рендера страницы (150–600) | 300 |
| `crop_margins` | Обрезка полей (мм) | 0 |

### 5.7. OCR-настройки

| Настройка | Tooltip-описание (кратко) | Значение по умолчанию |
|-----------|---------------------------|----------------------|
| `ocr.api.host` | IP-адрес ПК с Qwen3-VL в локальной сети | — |
| `ocr.api.port` | Порт HTTP API | 8000 |
| `ocr.api.timeout` | Макс. время ожидания ответа на одну страницу (сек) | 120 |
| `ocr.api.retries` | Повторы при ошибке сети | 3 |
| `ocr.language` | Подсказка языка для модели (`ru`, `en`, `auto`) | `auto` |
| `ocr.prompt` | Промпт: извлечь текст, заголовки, содержание | Шаблон из конфига |
| `ocr.page_parallel` | Параллельных страниц (осторожно с VRAM на сервере) | 1 |
| `ocr.output_format` | Формат страницы в MD (`plain`, `structured`) | `structured` |
| `ocr.detect_toc` | Искать и парсить содержание | Вкл |

### 5.8. Настройки PDF/A

| Настройка | Описание | По умолчанию |
|-----------|----------|--------------|
| `pdfa.level` | Уровень PDF/A: `2b` рекомендуется | `2b` |
| `pdfa.embed_fonts` | Встраивание шрифтов | Вкл |
| `pdfa.pdf_version` | Версия PDF | 1.7 |
| `pdfa.title`, `author` | Метаданные документа | Из имени файла |
| `pdfa.create_outline` | Создавать закладки из содержания | Вкл |
| `pdfa.outline_levels` | Макс. уровень заголовков в outline | 3 |
| `pdfa.text_opacity` | Прозрачность текстового слоя (0 = невидим) | 0 |
| `pdfa.keep_original` | Сохранять копию исходника в папке документа | Вкл |

### 5.9. Настройки векторизации (в контексте модуля 1)

| Настройка | Описание | По умолчанию |
|-----------|----------|--------------|
| `embed.api.host` / `port` | Подключение к Qwen3-Embedding-8B | — |
| `chunk.size` | Размер чанка в символах | 1000 |
| `chunk.overlap` | Перекрытие чанков | 200 |
| `chunk.strategy` | `fixed`, `paragraph`, `heading` | `paragraph` |
| `chunk.min_size` | Мин. размер чанка | 100 |
| `embed.batch_size` | Чанков за один API-запрос | 16 |
| `metadata.extract_via_llm` | Извлечение номера/наименования через узкий промпт к OCR-модели | Вкл |
| `metadata.require_confirmation` | Блокировать индексацию до подтверждения пользователем | Вкл |

### 5.10. UI модуля 1

**Макет (рекомендуемый):**

- Левая панель: настройки каталогов, кнопки «Открыть inbox», «Запустить/Остановить мониторинг».
- Центр: таблица задач (файл, статус, прогресс %, страница N/M, номер документа, наименование, статус подтверждения метаданных, ошибки).
- Правая панель: вкладки настроек (OCR, PDF, PDF/A, Embedding, Предобработка).
- Верхняя строка состояния: индикаторы **OCR API** | **Embedding API**.
- Нижняя панель: лог обработки (фильтр по уровню).

**Диалог переименования:** перед финальным перемещением в `documents` пользователь может изменить имя подкаталога (по умолчанию — имя PDF без расширения). Имя подкаталога **не заменяет** поле `document_name` в метаданных.

### 5.11. Обработка ошибок

| Ситуация | Поведение |
|----------|-----------|
| OCR недоступен | Задача в статусе «Ошибка», файл остаётся в `processing`, уведомление |
| Ошибка на странице K | Повтор до `retries`; при неудаче — пометка страницы в MD как `[OCR ERROR page K]` |
| Embedding недоступен | PDF/MD сохраняются; векторизация в очередь «отложенных» |
| Метаданные не подтверждены | Документ не индексируется; статус `pending_metadata`; доступен в модуле 2 для подтверждения |
| Дубликат документа | Предупреждение; опции: перезаписать, версионировать (`_v2`), пропустить |

---

## 6. Модуль 2 — Управление RAG-архитектурой

### 6.1. Назначение

Администрирование векторной базы, метаданных, файловой структуры документов; пере-embedding; синхронизация файловой системы и векторного индекса.

### 6.2. Функциональные требования

#### 6.2.1. Просмотр и редактирование метаданных

- Таблица документов: **номер документа**, **наименование документа**, имя папки, путь, число чанков, дата индексации, модель embedding, **статус подтверждения метаданных**.
- Редактирование полей метаданных документа и отдельных чанков (через форму).
- **Подтверждение метаданных:** для документов со статусом `pending_metadata` — форма с полями «Номер документа» и «Наименование документа» (предзаполнены автоматическими значениями); кнопки «Подтвердить» и «Сохранить и подтвердить» после редактирования. После подтверждения — запуск отложенной векторизации (если ещё не выполнена) и обновление метаданных всех чанков документа в vectorstore.
- Массовое редактирование тегов (опциональное поле `tags[]` в метаданных).

#### 6.2.2. Пере-embedding (re-embed)

- Выбор документов или всей коллекции.
- Параметры: новая модель (если сменился endpoint), размер чанка, overlap.
- Прогресс: документ / чанк / ETA.
- Атомарность: при сбое — откат к предыдущей версии индекса документа (snapshot).

#### 6.2.3. Файловая структура документов

- Древовидный проводник по `documents/` с операциями:
  - переименование папки/файла → обновление путей в метаданных и vectorstore;
  - удаление документа → удаление чанков из БД;
  - добавление внешнего MD/PDF → опциональная индексация без OCR.
- **Синхронизация:** кнопка «Проверить целостность» — сравнение registry.json, ФС и vectorstore; отчёт об осиротевших векторах и отсутствующих файлах.

#### 6.2.4. Управление коллекциями RAG

- Создание/удаление коллекций (для разных проектов).
- Экспорт/импорт метаданных (JSON).
- Статистика: общее число векторов, размер на диске, средняя длина чанка.

### 6.3. UI модуля 2

- Вкладка «Документы» — таблица + детали.
- Вкладка «Чанки» — просмотр текста чанка, соседние чанки, вектор (опционально t-SNE не требуется).
- Вкладка «Файлы» — TreeView с контекстным меню.
- Вкладка «Синхронизация» — отчёты, кнопки исправления.
- Вкладка «Настройки RAG» — chunking, embedding API, путь к vectorstore.
- Индикатор **Embedding API**.

### 6.4. Настройки модуля 2 (с tooltips)

| Элемент | Tooltip |
|---------|---------|
| Путь к vectorstore | Локальная папка ChromaDB/Qdrant. Не перемещайте во время индексации. Влияет на все модули поиска. |
| Коллекция по умолчанию | Имя коллекции для новых документов. Рекомендуется одна коллекция для одного архива. |
| Стратегия чанкинга | `paragraph` лучше для договоров; `fixed` — для однородного текста. Влияет на точность RAG. |
| Re-embed batch | Увеличивайте при стабильной сети и мощном сервере embedding. Снижайте при OOM на сервере. |
| Автосинхронизация путей | При переименовании в проводнике Windows вне приложения — предлагать ручную синхронизацию. |

---

## 7. Модуль 3 — Поисковый модуль (семантический поиск)

### 7.1. Назначение

Основной рабочий интерфейс **семантического поиска** по архиву документов **без LLM-агента**. Пользователь вводит поисковый запрос; модуль векторизует запрос, выполняет поиск по RAG-базе и отображает результаты в **табличном виде**: один запрос — одна таблица результатов.

### 7.2. Подключения (настройки модуля)

| Ресурс | Настройка |
|--------|-----------|
| Каталог документов | Путь к `documents/` |
| Векторная БД | Путь / имя коллекции |
| Embedding API | Для векторизации запроса (Qwen3-Embedding-8B) |

**Search LLM не используется** — в конфигурации модуля 3 отсутствуют параметры chat/completions и streaming.

### 7.3. Сценарий поиска

```
Запрос пользователя (строка поиска)
    → embedding(query) через LAN API
    → vectorstore.search(top_k, filters: metadata_confirmed=true)
    → для каждого результата:
          document_number, document_name — из метаданных документа
          summary — краткая вырезка текста чанка (фрагмент совпадения)
          materials — гиперссылка: имя файла document.pdf (PDF/A)
    → сортировка строк по убыванию релевантности (score)
    → отображение в таблице результатов
```

Один поисковый запрос формирует **ровно одну таблицу**. При новом запросе предыдущая таблица заменяется (опционально: история запросов в боковой панели).

### 7.4. Структура таблицы результатов

| Колонка | Источник данных | Описание |
|---------|-----------------|----------|
| **№ п/п** | Порядковый номер строки | 1, 2, 3… (не путать с номером документа) |
| **Номер документа** | `metadata.document_number` | Извлечён при обработке PDF, подтверждён пользователем |
| **Наименование документа** | `metadata.document_name` | Извлечено при обработке, подтверждено пользователем |
| **Краткое содержание ответа** | Текст чанка (`chunk_text`) | Вырезка фрагмента документа, по которому найдено семантическое совпадение с запросом; длина настраивается (`search.snippet_max_chars`, default 300 символов), с многоточием при обрезке |
| **Материалы** | `file_path` → `document.pdf` | Гиперссылка с отображаемым текстом = **имя файла PDF/A** (например, `document.pdf`); клик открывает PDF в системном просмотрщике на странице `page_start` (если поддерживается) |
| **Релевантность** | Score поиска | Опциональная колонка (скрываемая): числовое значение косинусной близости |

**Сортировка строк:** по убыванию релевантности (score). Пользователь может дополнительно сортировать по клику на заголовок колонки (номер, наименование).

**Дедупликация:** если несколько чанков одного документа попали в top-K — отображается **одна строка на документ** с наиболее релевантным чанком (настройка `search.dedupe_by_document`, default Вкл).

### 7.5. UI модуля 3 (макет)

```
┌────────────────────────────────────────────────────────────────────────┐
│  [●] Embedding API                              [⚙ Настройки поиска] │
├────────────────────────────────────────────────────────────────────────┤
│  [ Строка поиска........................................ ] [ Найти ]  │
├────────────────────────────────────────────────────────────────────────┤
│  Запрос: «условия поставки оборудования»          Найдено: 12          │
├──────────┬─────────────────────────┬──────────────────────┬──────────┤
│ Номер    │ Наименование документа  │ Краткое содержание   │ Материалы│
│ документа│                         │ ответа               │          │
├──────────┼─────────────────────────┼──────────────────────┼──────────┤
│ 12/34-24 │ Договор поставки...     │ ...поставщик обязуется│document.│
│          │                         │  поставить оборуд... │ pdf      │
├──────────┼─────────────────────────┼──────────────────────┼──────────┤
│ 05/11-23 │ Акт приёма-передачи     │ ...оборудование принято│document.│
│          │                         │  в соответствии...   │ pdf      │
└──────────┴─────────────────────────┴──────────────────────┴──────────┘
```

**Верхняя зона:**

- **Строка поиска** — поле ввода запроса; Enter или кнопка «Найти» запускают поиск.
- **Настройки** — кнопка/панель: `top_k`, порог релевантности, длина вырезки, фильтры по метаданным, путь к vectorstore.

**Центральная зона:**

- Таблица результатов текущего запроса (см. п. 7.4).
- Пустое состояние: подсказка «Введите запрос для поиска по архиву».
- Состояние загрузки: индикатор во время embedding и поиска.

**Дополнительно (опционально):**

- История запросов (последние N) в боковой панели — повторный клик восстанавливает запрос и таблицу.
- Экспорт таблицы результатов в CSV.

### 7.6. Настройки поиска

| Настройка | Описание | Default |
|-----------|----------|---------|
| `search.top_k` | Макс. число результатов (чанков/документов) | 20 |
| `search.score_threshold` | Мин. косинусная близость; строки ниже порога не показываются | 0.3 |
| `search.snippet_max_chars` | Макс. длина вырезки в колонке «Краткое содержание» | 300 |
| `search.snippet_highlight` | Подсветка ключевых слов запроса в вырезке | Выкл |
| `search.dedupe_by_document` | Одна строка на документ (лучший чанк) | Вкл |
| `search.only_confirmed` | Искать только документы с `metadata_confirmed=true` | Вкл |
| `search.filters` | JSON-фильтр по метаданным (номер, теги) | — |
| `search.mmr` | MMR diversification при множестве чанков одного документа | Выкл |
| `search.hybrid` | Гибрид: вектор + BM25 по MD (опционально фаза 2) | Выкл |

Параметры `llm.*`, `stream`, `system_prompt` **исключены** из модуля 3.

### 7.7. Tooltips для модуля 3 (обязательные)

Каждый элемент управления должен иметь tooltip формата:

> **Что это:** …  
> **Зачем:** …  
> **Рекомендация:** …  
> **Влияние:** …

Пример для `search.top_k`:

> **Что это:** Максимальное число результатов в таблице после одного запроса.  
> **Зачем:** Ограничивает объём выдачи по релевантности.  
> **Рекомендация:** 20 — для обзорного поиска; уменьшите для узких запросов.  
> **Влияние:** Больше K → длиннее таблица, возможны менее релевантные строки.

Пример для колонки «Краткое содержание ответа»:

> **Что это:** Фрагмент текста документа, семантически близкий к вашему запросу.  
> **Зачем:** Позволяет оценить релевантность без открытия PDF.  
> **Рекомендация:** Увеличьте длину вырезки в настройках, если фрагмент обрывается.  
> **Влияние:** Не является сгенерированным ответом LLM — только цитата из исходного текста.

---

## 8. Общие требования к GUI/UX

### 8.1. Внешний вид

- Современный стиль: тёмная и светлая тема (переключатель в настройках).
- Единая дизайн-система: отступы 8px grid, скругление 6–8px, акцентный цвет настраиваемый.
- Шрифты: **Segoe UI** (системный Windows), моноширинный для логов — **Cascadia Code**.
- Иконки: **Material Symbols** или **Fluent UI**-совместимый набор.
- Адаптивные панели: возможность изменять ширину split-панелей.

### 8.2. Автосохранение геометрии окон

**Обязательно** для главного окна и всех дочерних окон/диалогов:

- При изменении размера или позиции окна — сохранение в `config/ui_state.json`.
- При следующем запуске — восстановление размеров, позиции, состояния maximized.
- Сохранение ширины колонок таблиц и позиций splitters.

### 8.3. Tooltips

- Задержка появления: 500 мс.
- Многострочные tooltips для сложных настроек.
- Ссылка «Подробнее» в tooltip → открытие встроенной справки (опционально).

### 8.4. Главное окно приложения

- Навигация: боковая панель или вкладки: **Обработка PDF** | **RAG** | **Поиск**.
- Меню: Файл, Настройки, Справка, О программе.
- Глобальные настройки: пути, тема, язык UI (русский по умолчанию).

### 8.5. Доступность

- Клавиатурная навигация по основным элементам.
- Контраст не ниже WCAG AA для текста.
- Масштабирование UI: 100% / 125% / 150%.

---

## 9. Конфигурация приложения

### 9.1. Файл настроек

Путь: `%APPDATA%/Pdf_scan_and_ocr/settings.yaml`

Структура верхнего уровня:

```yaml
app:
  language: ru
  theme: dark
  data_root: D:/PdfArchive

modules:
  pdf_processor: { ... }
  rag_manager: { ... }
  search: { ... }

api:
  ocr: { host, port, timeout, ... }
  embedding: { host, port, ... }

ui_state:
  main_window: { x, y, width, height, maximized }
```

### 9.2. Профили API

Возможность сохранить несколько профилей подключения (например, «Домашний сервер», «Офис») с быстрым переключением.

### 9.3. Секреты

API-ключи (если потребуются) хранить в Windows Credential Manager или локальном зашифрованном файле, **не** в открытом YAML.

---

## 10. Нефункциональные требования

| Требование | Значение |
|------------|----------|
| ОС | Windows 10 (1903+), Windows 11 |
| RAM клиента | Мин. 8 ГБ (рекомендуется 16 ГБ) |
| Диск | Зависит от объёма архива; vectorstore ~1–2 КБ на чанк |
| Сеть | LAN 1 Гбит рекомендуется для OCR |
| Одновременная обработка | Настраиваемое число воркеров (default: 1 PDF) |
| Логи | Ротация 10 МБ × 5 файлов |
| Локализация | UI на русском; OCR-промпты настраиваемые |
| Установка | Инсталлятор MSI или portable ZIP |
| Обновления | Проверка версии на GitHub Releases (опционально) |

---

## 11. Безопасность

1. Все данные по умолчанию остаются на ПК пользователя.
2. API в LAN — без передачи в публичный интернет (настраиваемый `allow_external_hosts=false`).
3. Валидация путей: запрет записи вне `data_root` без подтверждения.
4. Логи не содержат полный текст документов (только идентификаторы и номера страниц).

---

## 12. Этапы разработки

### Этап 1 — Core + Модуль 1 (MVP)

- [ ] Каркас приложения, конфигурация, автосохранение окон
- [ ] File watcher, очередь задач
- [ ] Клиент OCR API, постраничная обработка
- [ ] Генерация MD и PDF с текстовым слоем
- [ ] Базовая конвертация PDF/A
- [ ] Извлечение номера и наименования документа, диалог подтверждения метаданных
- [ ] Индикаторы API

### Этап 2 — RAG

- [ ] Chunking, клиент Embedding API
- [ ] Интеграция ChromaDB/Qdrant
- [ ] Модуль 2: просмотр, подтверждение метаданных, синхронизация, re-embed

### Этап 3 — Поисковый модуль

- [ ] Модуль 3: строка поиска, embedding запроса, таблица результатов
- [ ] Колонки: номер документа, наименование, краткая вырезка, ссылка на PDF/A
- [ ] Сортировка по релевантности, подтверждённые метаданные в выдаче
- [ ] Полный набор tooltips

### Этап 4 — Полировка

- [ ] PDF Outline из содержания
- [ ] Темы, справка, инсталлятор
- [ ] Тесты, документация пользователя

---

## 13. Критерии приёмки

### Модуль 1

1. PDF, помещённый в `inbox`, автоматически обрабатывается без ручного запуска (при включённом мониторинге).
2. Итоговый файл в `documents/<имя>/` является PDF/A с возможностью поиска текста в Adobe Reader / Edge.
3. Файл `document.md` содержит полный распознанный текст всех страниц.
4. В `metadata.json` автоматически заполнены `document_number` и `document_name`; пользователь может подтвердить или изменить их в диалоге.
5. Векторы записаны в БД только после `metadata_confirmed=true`; поиск по чанкам возвращает результаты.
6. Индикаторы OCR и Embedding отражают реальное состояние API.

### Модуль 2

1. Переименование папки документа обновляет пути в метаданных и vectorstore.
2. Re-embed выбранного документа завершается без потери других документов.
3. Отчёт синхронизации выявляет рассогласования ФС и БД.
4. Пользователь может подтвердить или отредактировать номер и наименование документа для записей со статусом `pending_metadata`.

### Модуль 3

1. Ввод запроса в строке поиска формирует таблицу результатов (один запрос — одна таблица).
2. Таблица содержит колонки: номер документа, наименование документа, краткое содержание (вырезка текста), материалы (гиперссылка на PDF/A).
3. Строки отсортированы по убыванию релевантности.
4. Клик по ссылке в колонке «Материалы» открывает файл `document.pdf`.
5. Документы без подтверждённых метаданных не попадают в выдачу (при `search.only_confirmed=true`).
6. Все настройки модуля имеют информативные tooltips.
7. LLM-агент **не используется** — отсутствуют элементы чата и streaming.

### Общие

1. Приложение запускается на Windows 10 и 11 без установленного Python (после сборки).
2. Размеры и позиция главного окна восстанавливаются после перезапуска.

---

## 14. Риски и ограничения

| Риск | Митигация |
|------|-----------|
| Qwen3-VL 30B требует мощный GPU на сервере | Параметр `page_parallel=1`, очередь задач |
| Нестандартный API на сервере | Слой адаптеров + документация формата запроса |
| Большие PDF (500+ стр.) | Разбиение на батчи, возобновление с последней страницы |
| Качество OCR на плохих сканах | Блок предобработки, ручная повторная обработка страницы |
| PDF/A строгая валидация | Опциональная верификация через veraPDF (внешний инструмент) |

---

## 15. Глоссарий

| Термин | Определение |
|--------|-------------|
| **PDF/A** | Стандарт ISO для долгосрочного архивного хранения PDF |
| **OCR** | Optical Character Recognition — распознавание текста на изображении |
| **RAG** | Retrieval-Augmented Generation — в проекте: векторный поиск по чанкам документов (генерация ответа LLM не используется в модуле 3) |
| **Чанк** | Фрагмент текста фиксированного размера для векторизации |
| **Embedding** | Векторное представление текста в многомерном пространстве |
| **Вырезка (snippet)** | Краткий фрагмент текста чанка, отображаемый в результатах поиска |
| **metadata_confirmed** | Флаг подтверждения пользователем номера и наименования документа |
| **Outline** | Дерево закладок (содержание) внутри PDF |

---

## 16. Приложения

### Приложение А — Шаблон промпта извлечения номера и наименования документа

```
По распознанному тексту первых страниц документа извлеки:
1. Номер документа (регистрационный, исходящий, входящий — как указано в тексте).
2. Наименование документа (заголовок, тема).

Верни JSON:
{"document_number": "...", "document_name": "..."}
Если поле не найдено — верни пустую строку.
Текст:
{text_first_pages}
```

### Приложение Б — Шаблон OCR prompt

```
Распознай весь текст на изображении страницы документа.
Сохрани структуру: заголовки (уровни 1-3), абзацы, списки, таблицы в markdown.
Если на странице есть блок «Содержание» — выдели его отдельной секцией ## Содержание.
Верни только текст страницы без пояснений.
Язык: {language}
```

### Приложение В — Минимальные версии зависимостей (ориентир)

```
Python>=3.11
PySide6>=6.6
watchdog>=4.0
PyMuPDF>=1.24
pikepdf>=9.0
chromadb>=0.5
httpx>=0.27
pydantic-settings>=2.0
loguru>=0.7
```

---

**Конец документа**
